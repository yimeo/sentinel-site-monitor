# Project TODO

- [x] 设计监控任务、检查记录、SMTP 配置和告警状态的数据表及索引。
- [x] 实现监控任务的创建、编辑、暂停、删除、手动检查和任务校验接口。
- [x] 实现 URL 可达性检测、HTTP 状态码验证、期望内容匹配、响应时长记录和错误归类。
- [x] 实现安全的 SMTP 配置保存、连接测试、告警邮件和恢复邮件发送。
- [x] 实现告警去重状态机，确保连续故障仅通知一次并在恢复时通知。
- [x] 实现可由 Linux 系统 cron 或平台调度器安全调用的按分钟任务扫描入口。
- [x] 构建含实时状态总览、任务列表、状态筛选和快速操作的专业监控仪表盘。
- [x] 构建监控任务创建与编辑界面，支持 URL、名称、检查间隔、内容关键字和启停配置。
- [x] 构建按任务查询的检查历史界面，展示时间、结果、响应时长、HTTP 状态与错误信息。
- [x] 构建 SMTP 与通知收件人配置界面，支持连接测试与凭据掩码显示。
- [x] 在仪表盘或任务列表页新增按状态筛选（正常/异常/内容不符/待检查/已暂停）的 UI 与过滤逻辑，并确保空结果态可用。
- [x] 在仪表盘任务列表中加入快速操作（如立即检查、启停、进入编辑/详情），并处理操作中的加载与错误提示。
- [x] 在仪表盘或任务列表页新增按状态筛选（正常/异常/内容不符/待检查/已暂停）的 UI 与过滤逻辑，并确保空结果态可用。
- [x] 在仪表盘任务列表中加入快速操作（如立即检查、启停、进入编辑/详情），并处理操作中的加载与错误提示。
- [x] 在仪表盘任务列表补充启停与进入任务管理页的快捷操作，并提供操作反馈。
- [x] 在仪表盘任务列表提供直达对应检查历史的详情入口，并保持任务上下文。
- [x] 修复检查历史页收到无效 taskId 查询参数时的空白选择状态。
- [x] 编写监控状态判断、告警去重及输入校验的 Vitest 单元测试。
- [x] 验证项目类型、测试、管理界面和响应式布局，并编写 Ubuntu 20.04/CentOS 7 部署说明。
- [x] 为监控任务增加告警模式与连续提醒间隔配置，默认保留首次告警模式。
- [x] 为监控任务增加禁止内容规则，页面出现任一禁止文本时标记异常、记录历史并发送告警。
- [x] 扩展告警状态机，支持故障首次提醒、按设定间隔连续提醒，以及恢复即时通知。
- [x] 在任务创建、编辑与列表界面展示并配置禁止内容及连续提醒策略。
- [x] 编写连续提醒、禁止内容与恢复通知的 Vitest 测试，并更新部署说明。
- [x] 检查 163.5.85.122 的操作系统、宝塔面板、Web 服务、Node.js、数据库与端口占用情况。
- [x] 将构建后的应用部署至服务器，并通过宝塔的反向代理或现有 Web 服务安全暴露访问入口。
- [x] 在服务器配置数据库、应用服务守护、受令牌保护的定时检查及上线健康验证。
- [x] 将数据库访问和初始化迁移为 SQLite 本地文件存储，避免依赖服务器上的 MySQL。
- [x] 调整构建、测试与 Linux 部署说明以支持 SQLite，验证既有监控、告警、历史与配置功能。
- [x] 在 163.5.85.122 上通过宝塔代理最终验证管理员接口、任务列表和设置读取，确认 Web 管理界面可正常使用。
- [x] 验证至少一次真实 cron 周期执行结果，确认受令牌保护的定时检查会持续运行。
- [x] 复核 SQLite、CentOS 7、宝塔反向代理、systemd 与 cron 的部署说明已写入并符合实际配置。
- [x] 通过宝塔代理验证任务列表、通知设置、调度状态和历史记录接口可读取 SQLite 数据。
- [x] 通过宝塔代理补充验证历史记录接口在存在任务时可从 SQLite 正常读取检查数据。
- [x] 设计并实现本地管理员密码的安全哈希、修改校验和 Basic Auth 密码同步机制。
- [x] 增加告警邮件主题与正文模板的持久化配置，支持变量预览与告警/恢复邮件复用。
- [x] 增加可配置访问端口与基础访问地址的管理设置，并提供应用服务和宝塔 Nginx 配置同步方案。
- [x] 构建安全与通知设置页面，支持修改管理员密码、编辑邮件模板和查看端口/访问地址。
- [x] 域名绑定、HTTPS 与访问验证已按用户明确要求暂缓，待提供完整域名后再执行。
- [x] 编写认证、模板变量渲染和输入校验测试，部署并验证新增功能。
- [x] 通过受限的系统服务将界面保存的访问端口安全同步到宝塔 Nginx 与防火墙，同时保留 80 端口作为管理回退入口。
- [x] 为告警与恢复邮件模板增加使用示例变量的实时渲染预览。
- [x] 补充管理员密码、邮件模板与访问端口输入限制的 Vitest 覆盖。
- [x] 诊断并修复生产环境 SMTP 测试邮件发送失败的问题，完成真实发送验证。
- [x] 将告警与恢复邮件中的监控 URL 以安全的可点击 HTML 链接呈现，并保留纯文本回退内容。
- [x] 将期望内容与禁止内容的监控匹配调整为不区分英文字母大小写，并完成测试和生产部署。
- [x] 允许管理员在后台修改 HTTP Basic Auth 登录用户名，并安全同步宝塔 Nginx 认证文件。
- [x] 验证管理员用户名变更请求会触发 systemd 同步、更新 htpasswd，并可使用新用户名认证后安全回滚。
- [x] 补充管理员用户名同步请求格式与失败场景的测试覆盖。
- [x] 优先核验并交付恢复通知邮件中的故障持续时长展示功能。
- [x] 澄清需求：无需额外的管理员模板控制功能；后续需求为修改管理员登录用户名。
- [x] 在恢复通知邮件中显示故障持续时长，并增加可编辑模板变量与测试覆盖。
- [x] 将管理员登录名与本地管理员密码合并为同一个后台认证配置卡片。
- [x] 新增监控任务配置的 JSON 导出与安全导入功能，导入时只新增任务且不包含邮件、密码、历史或认证数据。
- [x] 制作不含任何服务器凭据、数据库和构建缓存的 Sentinel 源码包，并提供其他 Linux 服务器安装教程。
- [x] 扫描源码包敏感信息，并补充 pnpm/Corepack 安装前置与非宝塔 Nginx 脚本路径说明。
- [x] 确保每次监控检查重新解析域名，避免 DNS 指向变化后继续使用旧 IP，并记录解析地址变化。
- [x] 采集每次检查的 DNS 解析地址并在任务检查历史中记录地址变化，补充持久化与测试覆盖。
- [x] 将任务管理中的“必须出现”和“禁止出现”配置标签统一改为普通中性色，避免误用故障红色。
- [x] 将任务规则标签样式内聚到 Monitors 组件，并验证列表与编辑区域均使用普通中性色。
- [x] 制作包含最新功能的脱敏完整源码包，并提供可配置的一键 Linux 安装脚本与部署说明。
- [x] 为一键安装器补充 CentOS 7 的 Nginx 可用性兜底、受限认证文件权限及安装后健康检查。
- [x] 将 Sentinel 部署至 107.189.11.152，使用独立应用端口和新增 Nginx 站点配置，避免影响宝塔面板端口。
- [x] 在监控任务页面支持批量选择任务，并提供批量开启与批量暂停操作及操作反馈。
- [x] 在监控任务页面支持批量删除已选择任务，并提供二次确认、所有权隔离与操作反馈。
- [x] 修复导入或批量开启的监控任务未自动执行首次检查、持续显示“尚未检查”的问题。
- [x] 验证生产服务器 cron 实际自动触发一次，并确认现有导入任务的最近检查时间已更新。
- [x] 优化任务页批量操作按钮：增加小图标，并将“批量开启/暂停/删除”简化为“开启/暂停/删除”。
- [x] 在任务页增加“查看”批量立即检查操作，缩小批量操作按钮以避免响应式布局变形，并区分开启与查看图标。
- [x] 将任务页“开启”图标改为不同于“查看”的启用语义图标，避免视觉混淆。
- [x] 再次缩小任务页批量操作按钮，收紧图标与文字间距，将批量“查看”改为“检查”，并使用三角播放图标表示立即检查。
- [x] 将脱敏 Sentinel 源码发布至 yimeo 的 GitHub 公开仓库，并提供公开获取地址。
- [x] 将安装流程改为一条远程命令自动下载源码、安装依赖、配置服务、调度和反向代理。
- [x] 增加首次访问管理员初始化流程，并以独立页面替代当前嵌入式登录弹窗。
- [x] 验证本地部署下 Cookie 会话在初始化、登录与刷新后的持续认证链路。
- [x] 为首次初始化和本地登录增加会话签发与后续 auth.me 身份恢复的集成测试。
- [x] 将同频率导入任务的到期检查时间稳定分散到各自检查窗口内，避免集中请求。
- [x] 在任务管理页提供按需重新分散检查时间的安全操作，并补充调度与界面测试；桌面与移动端截图均已验证。
- [x] 将错峰调度版本更新至 107.189.11.152 的宝塔隔离部署，并核验服务、cron 和任务计划已生效；18080 入口已切换为应用层独立登录。
- [x] 修复本地独立登录成功后缺少提示且未自动跳转至管理界面的问题，并完成生产验证；43 项测试通过，107.189.11.152 已更新。
- [ ] 在浏览器中实际验证 107.189.11.152:18080 的本地登录成功提示、自动跳转至首页及刷新后的会话保持；代码和集成测试已完成，仍缺目标服务器浏览器现场记录。
- [x] 为本地登录与首次初始化补充前端集成测试，覆盖成功提示、路由跳转与受保护页面认证状态。
- [x] 为 Login/Setup 页面增加页面级集成测试，驱动 tRPC 成功响应并断言成功提示与跳转。
- [x] 补充 DashboardLayout 认证集成测试，确认登录成功后不再重定向回 /login。
- [ ] 在浏览器中实际验证 107.189.11.152:18080 的登录成功提示、自动跳转和刷新后的会话保持，并更新验证记录；现有用户反馈和测试不能替代现场浏览器回归。
- [ ] 诊断并修复 107.189.11.152 上 SMTP 测试邮件发送失败的问题，并验证实际送达。
- [ ] 诊断 465 与 587 SMTP 测试失败的具体错误，修复 TLS、认证或网络兼容问题并验证送达。
- [ ] 对照 163.5.85.122 的正常 SMTP 行为，定位 107.189.11.152 的网络出口或连接差异并修复。
- [x] 将 163.5.85.122 安全更新至最新 Sentinel 版本，保留 SQLite、SMTP、宝塔与 cron，并完成运行验证。
- [x] 在 163.5.85.122 更新后实际发送一次 SMTP 测试邮件，确认既有配置成功送达。
- [x] 补充 163.5.85.122 更新后的现场验证记录：服务、代理入口、cron 与 SMTP 测试均正常。
- [x] 安全重置 163.5.85.122 的本地管理员密码，验证新凭据并提示用户立即修改；公网登录与会话恢复均返回成功。
- [x] 已安全告知 163.5.85.122 的一次性管理员登录信息，并提示首次登录后立即修改密码；旧 Basic Auth 已切换为独立登录入口。
- [x] 将本轮独立登录修复、SMTP IPv4 兼容与新增测试安全同步至公开 GitHub 主分支并验证；远程 main 与本地发布副本均为 9a85167。
- [x] 优化错峰调度：在每分钟调度窗口内进一步分散到不同秒数，降低同一分钟内检查请求集中风险，并补充回归测试与部署验证；已更新 163.5.85.122 与 107.189.11.152。
- [x] 为 Login 页面补充可交互的页面级测试：提交用户名/密码，模拟 tRPC 成功返回，并断言成功提示、认证缓存写入和路由跳转顺序。
- [x] 为 Setup 页面补充可交互的页面级测试：提交初始化表单，模拟 tRPC 成功响应，并断言成功提示与跳转。
- [x] 补充 DashboardLayout 认证流测试：从未认证到已认证的状态变化中，验证登录成功后不会再次重定向回 /login。
- [x] 完成宝塔隔离远程安装脚本端到端验证；163.5.85.125 已在 df679fd 公开 main 上从 [1/8] 执行到“部署完成”，日志无 connection-refused 输出，并验证服务、18080 与新 cron 周期连续成功响应。
- [x] GitHub 写入权限已恢复，公开 main 已连续发布兼容修复，因此不再需要“GitHub 无法写入时的临时安装补丁”路径。
- [x] 修复宝塔隔离安装器在 CentOS 7 旧 GLIBC/GLIBCXX 环境下错误安装 Node.js 20 的问题；Node.js 16.20.2 已在 ai102 与 163.5.85.125 成功运行，并通过最终无噪声一键安装日志验证。

来源：用户在 ai102 执行安装器时的真实日志；cronie 已安装，但 Node.js 20.19.2 报 GLIBC/GLIBCXX/CXXABI 依赖缺失。
- [x] 修复 CentOS 7 上 Node.js 16 与项目 pnpm-lock.yaml lockfile 版本不兼容的问题：npm 生产依赖与预构建 dist 已在最终无噪声一键安装中验证可用。

- [x] 修复 CentOS 7 systemctl 启用 Sentinel 服务时的兼容性失败；安装器已选择 crond，163.5.85.125 已完成完整一键部署、服务启动与入口验证。

- [x] 修复 deploy-bt-isolated.sh 在 CentOS 7 上服务启动与 [8/8] 健康检查之间的时序问题；等待端口与静默重试逻辑已在 df679fd 全新安装中验证，脚本执行至“部署完成”。

- [x] 为宝塔隔离安装器补充 CentOS/RHEL firewalld 端口放行逻辑，并在 163.5.85.125 手动放行 18080 后从公网验证 Sentinel 入口可访问（HTTP 200）。

- [ ] 在启用 firewalld 的 CentOS/RHEL 宝塔服务器上重新执行最新 remote-install-bt-isolated.sh，验证安装器自动放行 18080/tcp 的分支实际生效。

- [x] 诊断并修复 SMTP 测试邮件的 535 authentication failed：确认原因为密码填写错误；用户修正密码后测试邮件发送成功，未记录邮箱密码或授权码。

来源：用户反馈。

- [x] 修复 Ubuntu 20.04 通用安装器在依赖安装阶段调用不存在的 pnpm：改为使用隔离 Node.js 目录的绝对 pnpm 路径并显式设置 HOME/PATH；已通过 Bash 语法检查、Ubuntu 安装器专项回归测试、54 项 Vitest 和生产构建。
- [x] 修复 Ubuntu 20.04 通用安装器与 lockfile v9/Vite 构建链的版本兼容性：Ubuntu 使用 Node.js 20 与 pnpm 10，CentOS 7 保持 Node.js 16 与兼容的 npm 生产依赖路径；163.5.85.125 已成功使用 Node.js 20.19.2 与 pnpm 10.4.1 安装。
- [x] 在已重装 Ubuntu 20.04 的 163.5.85.125 重新执行通用安装器，验证 systemd、Nginx、80 端口入口和 cron；安装已执行至“安装完成”。
- [x] 修复 Ubuntu 通用安装器在 80 端口替换默认 Nginx 站点时产生 duplicate default server 的问题，并在 163.5.85.125 验证 Nginx 配置与入口恢复正常；旧默认站点备份已移出 sites-enabled，nginx -t 与公网 HTTP 200 均通过。
- [x] 修复 Ubuntu 通用安装器错误启用 crond.service 的问题：直接尝试启用 cron.service，失败后回退 crond.service，完成 163.5.85.125 的 cron 调度恢复与验证；cron.service 为 active。
- [x] 修复通用安装器重装时未重启 site-monitor 导致新调度令牌返回 401 的问题，并在 163.5.85.125 验证首次调度请求成功；新周期日志持续返回 {"ok":true,"checked":0,"results":[]}。
- [ ] 在 163.5.85.125:80 真实打开 Sentinel，验证首次访问进入管理员初始化页，并完成初始化/登录流程的浏览器验收。
- [x] 修复 163.5.85.125 上从管理界面将外部访问端口切换至 18080 后未新增 18080 监听的问题：端口同步脚本已兼容 `listen 80 default_server`；Nginx 已监听 18080，本机与公网 `http://163.5.85.125:18080/` 均返回 HTTP 200。80 保留为安装期回退入口。
- [ ] 在 163.5.85.125 的设置页再次保存外部访问端口 18080，记录保存成功提示、请求文件生成、同步服务触发、Nginx 监听与公网 HTTP 200 的完整端到端验收；用户已确认修复后的 18080 可访问，但尚未验证再次通过设置页保存的流程。
- [x] 按用户要求在 163.5.85.125 停止暴露 Sentinel 的 80 端口，仅保留 18080 监听，并验证公网 18080 返回 HTTP 200、80 的本机连接被拒绝且公网不再返回 Sentinel 响应。
- [x] 优化访问设置中的域名输入：允许管理员仅填写域名或主机名，自动规范化为可用访问地址，无需手工输入 `http://` 或 `https://`；兼容误输入的协议前缀。
- [x] 新增自定义 SSL 证书、私钥及可选证书链的安全管理界面与后端校验，私钥不写入 SQLite、不在页面回显，并限制为 HTTPS 管理页面提交。
- [x] 扩展端口同步服务，根据域名和已验证的自定义证书安全生成 Nginx HTTPS 配置，支持证书有效期/私钥匹配检查及 Nginx 校验失败回滚。
- [ ] 在 163.5.85.125 部署域名/SSL 功能，并在用户提供域名解析和证书材料后验证 HTTPS 访问；应用、TLS 受限同步脚本和 systemd 路径监视器已部署，待真实证书现场验收。

现场记录：ai102 首次失败发生在 `[4/8] 安装依赖并构建`，日志为 `runuser: failed to execute pnpm: No such file or directory`；此前宝塔隔离脚本因未找到 `/www/server/nginx/sbin/nginx` 正确拒绝执行，Ubuntu 应使用通用 `remote-install.sh`。
